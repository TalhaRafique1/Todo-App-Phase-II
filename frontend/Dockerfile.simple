FROM nginx:alpine

# Copy the simple HTML file to nginx's default directory
COPY index.html /usr/share/nginx/html/

# Expose port 80 (which we'll map to 3000)
EXPOSE 80

# The container will start nginx automatically